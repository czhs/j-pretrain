# DECISIONS

Concise scientific/engineering decisions. Format: date | decision | evidence | alternatives | reason | frozen?

## 2026-07-21
- **Reduced scope authorized**: Only the 300M MusicPile subset of Fig 3a (not 30M/150M/300M sweep). | Evidence: mission doc explicitly authorizes. | Alt: full 3-size sweep. | Reason: compute budget on single 4090; user pre-authorized. | FROZEN (record in SCOPE_LOCK.json).
- **Datasets**: C4=allenai/c4, MusicPile=m-a-p/MusicPile, ChemPile=jablonkagroup/chempile-education (education split reachable). | Evidence: HF dataset_info OK for all three. | Reason: match paper D_pre/D_post/D_ft. | Pending: confirm exact ChemPile subset from paper/official code before freezing.
- **Env**: Build dedicated conda env; base env lacks ML stack. | Reason: reproducibility + isolation. | Not yet frozen (env hash pending). Env `jpre` now built: py3.11.15, torch 2.5.1+cu121, transformers 4.46.3, datasets 3.1.0, tokenizers 0.20.3, safetensors 0.4.5, accelerate 1.1.1, numpy 2.1.3, pandas 2.2.3, scipy 1.14.1, matplotlib 3.9.2, pyarrow 18.0.0, wandb 0.18.7, pytest 8.3.3.

## 2026-07-21 (spec freeze + benchmark milestone)
- **Paper spec extracted + independently audited → FREEZE-READY.** | Evidence: docs/PAPER_SPEC.md written by extraction subagent; reports/SPEC_AUDIT.md by independent auditor = 11/11 claims CONFIRMED, zero WRONG/OVERSTATED. | FROZEN pending spec_hash record.
- **Model architecture = official SmolLM2-135M config** (30L/576h/9 heads/3 KV GQA/1536 MLP/49152 vocab/rope 1e5/rms 1e-5/tied/no-bias/init 0.0416667/silu). | Evidence: paper gives NO 135M arch table (only 1B Table 2); "SmolLM2-style 135M" §3.3; HF config verified via curl. Local build = 134.52M params (tied). | Alt: MHA "interpolated" variant like paper's 1B. | Reason: closest official match; paper silent. | Tag INFERRED_FROM_SMOLLM2; FROZEN.
- **Stage-1 C4 budget = 8.7B tokens/run (NOT literal 10B).** | Evidence: Table 3 precise 8.7B vs §3.3 "~10B" loose prose; audit confirms. | Reason: dataset-specific table authoritative. | FROZEN. Disclose both in reports.
- **λ scheduling = ADD (C4 fixed 8.7B + λ·300M MusicPile interleaved), NOT compute-matched, NOT C4-replacement.** | Evidence: §4.2 explicitly says Fig 3a does not control total D_post seen (that is Fig 3b); mission doc default = keep C4 fixed, add λ MusicPile. Code agent noted "replace C4 within 10B" is *consistent with* text but not stated. | Alt: replace C4 tokens to hold total ~10B (rejected: that shrinks C4 as λ grows, against mission default). | Reason: paper ambiguous on within-stage replace vs add; mission doc resolves to ADD. | FROZEN; documented as a limitation. Interleave = uniform-throughout at target mixture ratio (front-loading UNKNOWN).
- **Stage-2 fixed Fig-3a config (LOCAL, until code): LR 5e-4, dropout 0.0, global batch 480, warmup 500, WD 0.1, min LR 5e-5, max 2B tokens, early stop on MusicPile val (patience 3, eval ~100 steps).** | Evidence: Table 7 sweep ranges; single chosen triple UNKNOWN. | Reason: mid-range LR, standard. To be finalized via a λ=0-only preregistered pilot per mission doc before locking. | NOT yet frozen — pilot pending.
- **Execution: torch.compile primary (bf16, SDPA, fused AdamW), eager fallback.** | Evidence: bench mb=8 seq1024: eager 52.5k tok/s @16.6GB reserved; compile 94.3k tok/s @9.65GB (fuses large-vocab CE, cuts time+memory). | Reason: 1.8x throughput, big margin, lower VRAM. | Execution-only param (not scientific); FROZEN as default, per-run reproducible.
- **Microbatch (execution): mb=8 seq1024 baseline** (94k tok/s compile; room to grow given 9.65GB). Global batch achieved via grad accumulation to match frozen scientific global-batch-tokens. | Execution-only; tune during real impl, preserve effective global batch across λ.
- **OPERATIONAL (critical): invoke env Python by direct path `/home/hshi-j-4090/miniconda3/envs/jpre/bin/python`, NOT `conda run -n jpre`.** | Evidence: `conda run` + heredoc silently swallows stdout (empty output, exit 0) — caused the earlier "hf_hub_download hang" false alarm. Direct path works and prints normally. | Also satisfies mission requirement to invoke the env interpreter explicitly in all orchestration/tmux/training commands. | FROZEN convention. Set HF_HUB_ENABLE_HF_TRANSFER=0, TOKENIZERS_PARALLELISM=false.
- **Data access VALIDATED (streaming, this iteration).** | C4 (`allenai/c4` name=`en`): key `text` OK. MusicPile (`m-a-p/MusicPile`): key `text`, format is ChatMusician "Human: ... Assistant: ..." conversational text. ChemPile: `jablonkagroup/chempile-education` REQUIRES a config; has 4: LibreText_Chemistry-default, mit-ocw-lecture-transcripts-default, us-olympiad-problems-default, youtube-transcripts-as-lectures-default. | Tokenizer SmolLM2-135M confirmed: vocab 49152, bos=eos=0, pad=None (must set pad handling for eval).
- **ChemPile config selection: PENDING (decide in DATA_AUDIT).** | Options: combine all 4 education configs to reach 0.3B-token chemistry FT corpus (need 200M FT + val). Paper says only "ChemPile / Chemistry-domain, 0.3B". | Not yet frozen.
- **Feasibility (preliminary, pre-full-impl): PASS.** Stage1 44.25B tok, Stage2 ≤10B, Stage3 1B. @94k tok/s ≈7 days compute; @52k eager ≈15 days. Both < 21-day gate. Final FEASIBILITY.md after full impl + real checkpoint-size bench. | Do NOT reduce scope.

## 2026-07-21 (data pipeline milestone)
- **Tokenizer FROZEN into repo** (`configs/data/tokenizer/`, rev 93efa2f0, dir sha b4ec3f78…a301c4, 4.6M). Loaded only from local copy, never network. | vocab 49152, bos=eos=0, pad=None, uint16 tokens. | FROZEN.
- **Dataset revisions PINNED**: C4 1588ec45, MusicPile 5930bd71, ChemPile-education c653c3c7. Passed as `revision=` on every load_dataset. | Evidence: HfApi.repo_info. | FROZEN.
- **ChemPile config = ALL 4 education configs** (LibreText_Chemistry, mit-ocw-lecture-transcripts, us-olympiad-problems, youtube-transcripts-as-lectures), concatenated in that deterministic order. | Evidence: chempile-education requires a config; paper only says "ChemPile 0.3B chemistry". | Alt: single config (rejected: too small / arbitrary). | LOCAL_REPRODUCTION_CHOICE; FROZEN; disclose.
- **Splits**: C4 uses native train/validation HF splits; MusicPile+ChemPile carve val/tune by per-mille document-hash bands (val [0,5), tune [5,10), train rest), salted per corpus. Disjoint by construction; splitting per-document before packing => no window straddles splits. | FROZEN.
- **D_post 300M subset = window prefix [0,292968) of ordered packed MusicPile-train** = 299,999,232 tokens (floored to whole 1024-windows). Same range for Stage-1 exposure + Stage-2. Nested (30M⊂150M⊂300M). Requires MusicPile pool ≫300M (≈4.16B total; ample). | LOCAL; FROZEN.
- **Lambda interleave = ADD, centered-even distribution**: fixed 8.7B C4 (8,496,093 win, identical across λ) + prefix λ·300M MusicPile at slots floor((j+0.5)T/n_mp). Uniform (first+last Stage-1 window are C4); C4 relative order byte-identical across λ (only inserted MP differs). MP prefix => each selected token shown ≤1× in Stage 1. Exact MP tokens: 0/75M/150M/225M/300M. | Front-loading vs uniform UNKNOWN in paper; uniform = documented LOCAL choice + limitation. | FROZEN.
- **Artifact root = `/home/hshi-j-4090/Desktop/j-pretrain-artifacts`** (outside repo, on sda2 581G free). Recorded in experiment_state.json. | Set via J_PRETRAIN_ARTIFACT_ROOT (driver default matches). | FROZEN unless migration.
- **Probes = first 256 val windows/corpus** -> artifacts/probes/{c4,musicpile,chempile}/ + probe_manifest.json (frozen token IDs, same for every checkpoint). Interpretability out of scope (preserve only). | FROZEN.
- Data pipeline: 29 unit tests (tests/test_data.py) + smoke-validated end-to-end driver (all 6 (source,split) sets built, checksums verify, mmap load OK). Full tokenization deferred to detached run after readiness audit.
- **Training precision = BF16 mixed** (fp32 master params + bf16 autocast on CUDA, NO GradScaler — bf16 has fp32 range). CPU/test path = plain fp32. | Evidence: Table 5 "BF16 mixed precision" (EXPLICIT). Resumable stores fp32 model+opt exactly; analysis snapshot = bf16 safetensors (matches training-compute repr). | FROZEN.
- **Data order = pure function of an integer cursor** (windows_consumed). Stage1 = O(log n) interleave lookup (Stage1Plan), Stage2/3 = per-epoch deterministic permutation (ShuffledSourcePlan, seed*1_000_003+epoch). No dataloader-worker state to serialize; resume restores cursor+model+opt+RNG only => byte-exact (verified by deterministic resume test). C4 Stage-1 order NOT shuffled (packed order, identical across λ, requirement met). | FROZEN.
- **Stage configs frozen** (configs/stage{1,2,3}/*.json as StageConfig). Stage1 (LOCAL, from 1B ref): peak 5e-4/min 5e-5/warmup 1000/global-batch 512 seq/WD 0.1; cosine horizon PER-RUN (total steps = plan windows//gbs; peak/min/warmup identical across λ) — natural under ADD policy since total tokens vary with λ. Stage3 (peak 5e-5 EXPLICIT + 200M EXPLICIT; rest LOCAL: min 5e-6/warmup 100/gbs 480/WD 0.1). Stage2 PROVISIONAL (LR 5e-4/db 0/gbs 480/warmup 500/WD 0.1/min 5e-5/max 2B/ES patience 3) pending λ=0 pilot. | Stage1/3 FROZEN; Stage2 provisional.
- **SCOPE LOCKED** (state/SCOPE_LOCK.json, scope_hash ca992877). 5 conditions: music-300m_lambda-{0.0,0.25,0.5,0.75,1.0}, seed 1234. Fig 3a (early exposure), NOT 3b. Reductions disclosed: 300M-only, one-seed, LOCAL optimizer knobs. Feasibility gate PASS (compute+storage). | Immutable per mission rules. FROZEN.
- **Checkpoint sizes MEASURED**: analysis 310.6 MB (bf16 safetensors; tied embed stored 2× by safetensors, +56 MB, kept for load-independence), resumable 1850 MB. Projection ~308 GB w/headroom < 581 GB free. | reports/FEASIBILITY.md + docs/STORAGE_PLAN.md.
